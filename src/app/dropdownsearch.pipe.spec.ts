import { DropdownsearchPipe } from './dropdownsearch.pipe';

describe('DropdownsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
